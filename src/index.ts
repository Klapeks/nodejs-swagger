import Swagger from "./swagger"
import { SwaggerInfo, SwaggerPath, SwaggerFile, SwaggerPathInfo } from './types';

export {
    SwaggerInfo,
    SwaggerPath,
    SwaggerFile,
    SwaggerPathInfo
}

export default Swagger;